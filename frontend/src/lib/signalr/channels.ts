/** Logical SignalR channel names — mirror the backend DashboardChannels constants. */
export const DASHBOARD_CHANNELS = {
  systemLogs: "system.logs",
  heartbeat: "heartbeat",
  terminalResponse: "terminal.response",
} as const;
